
const eventCal = (score)=>{

    // Calculate event from score
    // 1.Has event
    // 2.No event

    return {
        device_id: "TestID",
        description: ["I guess it right", "I'm not sure"],
        factors: ["co2","pm2_5"],
    }
}
module.exports = eventCal